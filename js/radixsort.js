export function radixSort(valuesArray, d) {
    let n = valuesArray.length;
    let largest = Math.max(valuesArray);
    while (largestNum/d > 0) {
        //....
    }
}
function getMax(valuesArray){
    let max = 0;
    for (let i = 0; i < valuesArray.length; i++) {
        if (max < valuesArray[i]) {
            max = valuesArray[i];
        }
    }
    return max;
}
